{
    "version": 2,
    "builds": [
        {
            "src": "src/server.js",
            "build": "next build && npm run migrate",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "(.*)",
            "dest": "src/server.js"
        }
    ]
}